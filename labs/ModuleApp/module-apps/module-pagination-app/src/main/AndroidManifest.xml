<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="module.pagination.app" >

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".PaginationHomeActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PhotoListActivity" android:screenOrientation="portrait" />
        <activity android:name=".PhotoGridActivity" android:screenOrientation="portrait" />

        <activity android:name=".photos.PhotoListActivity" android:screenOrientation="portrait" />
        <activity android:name=".photos.PhotoSwiperActivity" android:screenOrientation="portrait" />

    </application>

</manifest>
